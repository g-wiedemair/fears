[package]
name = "feap_utils"
version.workspace = true
edition.workspace = true
rust-version.workspace = true


[features]
default = ["parallel"]

parallel = ["feap_core/std"]

std = []

debug = []


[dependencies]
feap_core = { path = "../feap_core" }
